[Unit]
Description=tmux session for user %I
After=graphical-session.target

[Service]
Type=forking
User=%I
ExecStart=/usr/bin/tmux new-session -s %I -d
ExecStiop=/usr/bin/tmux kill-session -t %I

[Install]
WantedBy=graphical-session.target
